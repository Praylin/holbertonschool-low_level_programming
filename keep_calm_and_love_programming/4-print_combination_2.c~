#include <stdio.h>

void print_combination_2(void)
{
  int i,j;
  for (i = 0; i <= 9; i++)
    {
      /* printf("%d",i);*/
      for (j = i+1; j <= 9; j++)
	{
	  printf("%d%d",i,j);
	  printf("%c",',');
	}
    }
}
