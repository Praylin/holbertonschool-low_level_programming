char *cap_string(char *s)
{
  int i;
  for(i = 0; s[i] != '\0'; i++)
    {
      if(s[i] >=97 && s[i] <=122)
	{
	  if(s[i] == ' ' || s[i] == '\n' || s[i] == '\t')
	    s[i+1] = s[i+1] - 32;
	}
    }
  return s;
}
